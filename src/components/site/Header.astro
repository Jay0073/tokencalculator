---
import BrandMark from './BrandMark.astro';
import ProviderIcon from './ProviderIcon.astro';
import UiIcon from './UiIcon.astro';
import { localeNames, localeShort, locales, localizedPath, type Locale } from '../../i18n/config';
import { homeCopy } from '../../i18n/home';
import { navCopy } from '../../i18n/nav';
interface Props { locale?: Locale }
const { locale='en' } = Astro.props;
const copy=homeCopy[locale];
const nav=navCopy[locale];
const currentPath=Astro.url.pathname;
const hasLocalizedEquivalent=currentPath==='/' || locales.some((item)=>currentPath===`/${item}/`);
const localeDestination=(item:Locale)=>localizedPath(hasLocalizedEquivalent?currentPath:'/',item);
---
<header class="site-header relative z-20">
  <div class="shell header-shell h-[80px] grid grid-cols-[1fr_auto] lg:grid-cols-[1fr_auto_1fr] items-center gap-2 sm:gap-5">
    <a href={localizedPath('/',locale)} class="flex items-center gap-3.5 no-underline w-max" aria-label="TokenCalculator.dev home">
      <BrandMark size={36}/>
      <span class="font-semibold tracking-[-.04em] text-[17px] sm:text-[18px]">TokenCalculator<span class="text-[var(--accent)]">.dev</span></span>
    </a>
    <nav class="hidden lg:flex items-center justify-center gap-1" aria-label={nav.calculators}>
      <details class="mega-menu relative" data-header-menu>
        <summary class="nav-provider cursor-pointer list-none">
          {nav.calculators}
          <svg class="mega-chevron" width="13" height="13" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m3 4.5 3 3 3-3"/></svg>
        </summary>
        <div class="mega-popover fixed left-1/2 top-[80px] z-50 w-[min(680px,calc(100vw-32px))] -translate-x-1/2 overflow-hidden rounded-2xl border hairline bg-[var(--panel-raised)] shadow-[var(--shadow)]">
          <div class="mega-grid">
            <section aria-labelledby="mega-input-title">
              <p class="mega-label" id="mega-input-title">{nav.inputAndPlanning}</p>
              <a class="mega-link" href={localizedPath('/',locale)}>
                <span class="mega-icon"><UiIcon name="calculator"/></span>
                <span><strong>{nav.tokenCalculator}</strong><small>{nav.tokenCalculatorDetail}</small></span>
              </a>
              <a class="mega-link" href="/tokens-to-words-converter/">
                <span class="mega-icon"><UiIcon name="convert"/></span>
                <span><strong>{nav.tokensToWords}</strong><small>{nav.tokensToWordsDetail}</small></span>
              </a>
              <a class="mega-link" href="/estimators/">
                <span class="mega-icon"><UiIcon name="cost"/></span>
                <span><strong>{nav.costLab}</strong><small>{nav.costLabDetail}</small></span>
              </a>
            </section>
            <section aria-labelledby="mega-provider-title">
              <p class="mega-label" id="mega-provider-title">{nav.providerCalculators}</p>
              <a class="mega-link mega-link-compact" href="/openai-token-counter/"><span class="mega-icon"><ProviderIcon provider="openai"/></span><span><strong>OpenAI</strong></span></a>
              <a class="mega-link mega-link-compact" href="/claude-token-counter/"><span class="mega-icon"><ProviderIcon provider="anthropic"/></span><span><strong>Claude</strong></span></a>
              <a class="mega-link mega-link-compact" href="/gemini-token-counter/"><span class="mega-icon"><ProviderIcon provider="google"/></span><span><strong>Gemini</strong></span></a>
              <a class="mega-link mega-link-compact" href="/deepseek-token-counter/"><span class="mega-icon"><ProviderIcon provider="deepseek"/></span><span><strong>DeepSeek</strong></span></a>
            </section>
          </div>
          <a class="mega-footer-link" href="/estimators/">{nav.providerCalculatorsDetail} <span aria-hidden="true">→</span></a>
        </div>
      </details>
      <details class="mega-menu relative" data-header-menu>
        <summary class="nav-provider cursor-pointer list-none">
          {nav.explore}
          <svg class="mega-chevron" width="13" height="13" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m3 4.5 3 3 3-3"/></svg>
        </summary>
        <div class="mega-popover fixed left-1/2 top-[80px] z-50 w-[min(680px,calc(100vw-32px))] -translate-x-1/2 overflow-hidden rounded-2xl border hairline bg-[var(--panel-raised)] shadow-[var(--shadow)]">
          <div class="mega-grid">
            <section aria-labelledby="mega-learn-title">
              <p class="mega-label" id="mega-learn-title">{nav.learnAndVerify}</p>
              <a class="mega-link" href="/docs/">
                <span class="mega-icon"><UiIcon name="book"/></span>
                <span><strong>{nav.documentation}</strong><small>{nav.documentationDetail}</small></span>
              </a>
              <a class="mega-link" href="/methodology/">
                <span class="mega-icon"><UiIcon name="methodology"/></span>
                <span><strong>{nav.methodology}</strong><small>{nav.methodologyDetail}</small></span>
              </a>
              <a class="mega-link" href="/docs/privacy-architecture/"><span class="mega-icon"><UiIcon name="shield"/></span><span><strong>{nav.privacyArchitecture}</strong><small>{nav.privacyArchitectureDetail}</small></span></a>
            </section>
            <section aria-labelledby="mega-company-title">
              <p class="mega-label" id="mega-company-title">{nav.resources}</p>
              <a class="mega-link mega-link-compact" href="/blog/"><span class="mega-icon"><UiIcon name="blog"/></span><span><strong>{nav.engineeringBlog}</strong></span></a>
              <a class="mega-link" href="/docs/api/"><span class="mega-icon"><UiIcon name="api" size={20}/></span><span><strong>{nav.apiReference}</strong><small>{nav.apiReferenceDetail}</small></span></a>
              <a class="mega-link mega-link-compact" href="/about/"><span class="mega-icon"><UiIcon name="info"/></span><span><strong>{copy.about}</strong></span></a>
              <a class="mega-link mega-link-compact" href="/contact/"><span class="mega-icon"><UiIcon name="mail"/></span><span><strong>{copy.contact}</strong></span></a>
            </section>
          </div>
        </div>
      </details>
      <a class="nav-provider" href="/integrations/" aria-current={currentPath==='/integrations/'?'page':undefined}><UiIcon name="plug" size={17}/>{nav.integrations}</a>
      <a class="nav-provider nav-api" href="/developers/api/" aria-current={currentPath==='/developers/api/'?'page':undefined}>
        <UiIcon name="api" size={19}/>
        {nav.developers}
      </a>
    </nav>
    <div class="flex items-center justify-end gap-1.5 sm:gap-2">
      <span class="privacy-pill hidden xl:inline-flex h-9 shrink-0 items-center gap-1.5 whitespace-nowrap rounded-full border px-2.5 text-sm font-semibold" title={copy.privacyDetail}>
        <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 3 5 6v5c0 4.6 2.8 8 7 10 4.2-2 7-5.4 7-10V6l-7-3Z"/><path d="m9.5 12 1.7 1.7 3.5-3.7"/></svg>
        <span>{copy.privacyFirst}</span>
      </span>
      {/* Narrow on phones: the full name runs to "Português (Brasil)", which forced the
          control wide enough to unbalance the row. The code carries the same meaning. */}
      <details id="language-menu" class="language-menu relative w-auto sm:w-[128px]">
        <summary class="flex h-9 cursor-pointer list-none items-center rounded-full border hairline bg-[var(--panel)] px-2 sm:px-2.5 text-sm font-semibold text-[var(--muted)] transition-colors hover:text-[var(--text)]" aria-label={copy.language}>
          <svg class="shrink-0" width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><circle cx="12" cy="12" r="9"/><path d="M3 12h18M12 3c2.2 2.5 3.3 5.5 3.3 9S14.2 18.5 12 21M12 3c-2.2 2.5-3.3 5.5-3.3 9S9.8 18.5 12 21"/></svg>
          <span class="px-1 font-mono text-xs sm:hidden">{localeShort[locale]}</span>
          <span class="hidden min-w-0 flex-1 truncate px-1.5 sm:inline">{localeNames[locale]}</span>
          <svg class="language-chevron shrink-0 transition-transform duration-200" width="12" height="12" viewBox="0 0 12 12" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m3 4.5 3 3 3-3"/></svg>
        </summary>
        <div class="language-popover absolute right-0 top-[calc(100%+.55rem)] z-50 w-[188px] overflow-hidden rounded-xl border hairline bg-[var(--panel-raised)] p-1.5 shadow-[var(--shadow)]" role="menu" aria-label={copy.language}>
          {locales.map((item)=><a class="language-option relative flex min-h-9 items-center rounded-lg px-3 text-sm no-underline" data-active={item===locale ? 'true' : 'false'} data-locale-name={localeNames[item]} href={localeDestination(item)} role="menuitem" aria-current={item===locale ? 'page' : undefined}><span>{localeNames[item]}</span>{item===locale && <svg class="ml-auto text-[var(--accent)]" width="14" height="14" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m3.5 8.5 3 3 6-7"/></svg>}</a>)}
        </div>
      </details>
      <button id="theme-transition-toggle" class="size-9 grid place-items-center border hairline rounded-full bg-[var(--panel)] text-[var(--muted)] hover:text-[var(--text)]" type="button" aria-label={copy.theme}>
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true"><path d="M12 3a9 9 0 1 0 9 9c-5 2-11-4-9-9Z"/></svg>
      </button>

      {/* The menu trigger sits last so it is the rightmost control, under the thumb.
          The panel mirrors the desktop mega-menu: same grouping, same icons, same
          descriptions, so the two navigations teach the same structure. Plain
          <details> keeps every link in the markup and needs no JavaScript. */}
      <details class="mobile-menu lg:hidden" data-header-menu>
        <summary class="size-9 grid place-items-center border hairline rounded-full bg-[var(--panel)] text-[var(--muted)] hover:text-[var(--text)] cursor-pointer list-none" aria-label={nav.openMenu}>
          <svg class="mobile-menu-open" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M3 6h18M3 12h18M3 18h18"/></svg>
          <svg class="mobile-menu-close" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true"><path d="M5 5l14 14M19 5 5 19"/></svg>
        </summary>
        <nav class="mobile-menu-panel" aria-label={nav.calculators}>
          <p class="mobile-menu-label">{nav.inputAndPlanning}</p>
          <a class="mobile-menu-item" href={localizedPath('/',locale)} aria-current={currentPath===localizedPath('/',locale)?'page':undefined}>
            <span class="mobile-menu-icon"><UiIcon name="calculator"/></span>
            <span><strong>{nav.tokenCalculator}</strong><small>{nav.tokenCalculatorDetail}</small></span>
          </a>
          <a class="mobile-menu-item" href="/tokens-to-words-converter/" aria-current={currentPath==='/tokens-to-words-converter/'?'page':undefined}>
            <span class="mobile-menu-icon"><UiIcon name="convert"/></span>
            <span><strong>{nav.tokensToWords}</strong><small>{nav.tokensToWordsDetail}</small></span>
          </a>
          <a class="mobile-menu-item" href="/estimators/" aria-current={currentPath==='/estimators/'?'page':undefined}>
            <span class="mobile-menu-icon"><UiIcon name="cost"/></span>
            <span><strong>{nav.costLab}</strong><small>{nav.costLabDetail}</small></span>
          </a>

          <p class="mobile-menu-label">{nav.providerCalculators}</p>
          <div class="mobile-menu-providers">
            <a href="/openai-token-counter/" aria-current={currentPath==='/openai-token-counter/'?'page':undefined}><ProviderIcon provider="openai" size={17}/>OpenAI</a>
            <a href="/claude-token-counter/" aria-current={currentPath==='/claude-token-counter/'?'page':undefined}><ProviderIcon provider="anthropic" size={17}/>Claude</a>
            <a href="/gemini-token-counter/" aria-current={currentPath==='/gemini-token-counter/'?'page':undefined}><ProviderIcon provider="google" size={17}/>Gemini</a>
            <a href="/deepseek-token-counter/" aria-current={currentPath==='/deepseek-token-counter/'?'page':undefined}><ProviderIcon provider="deepseek" size={17}/>DeepSeek</a>
          </div>

          <p class="mobile-menu-label">{nav.learnAndVerify}</p>
          <a class="mobile-menu-item compact" href="/docs/"><span class="mobile-menu-icon"><UiIcon name="book"/></span><span><strong>{nav.documentation}</strong></span></a>
          <a class="mobile-menu-item compact" href="/methodology/"><span class="mobile-menu-icon"><UiIcon name="methodology"/></span><span><strong>{nav.methodology}</strong></span></a>
          <a class="mobile-menu-item compact" href="/docs/privacy-architecture/"><span class="mobile-menu-icon"><UiIcon name="shield"/></span><span><strong>{nav.privacyArchitecture}</strong></span></a>
          <a class="mobile-menu-item compact" href="/blog/"><span class="mobile-menu-icon"><UiIcon name="blog"/></span><span><strong>{nav.engineeringBlog}</strong></span></a>

          <p class="mobile-menu-label">{nav.developers}</p>
          <a class="mobile-menu-item compact" href="/docs/api/"><span class="mobile-menu-icon"><UiIcon name="api" size={19}/></span><span><strong>{nav.apiReference}</strong></span></a>
          <a class="mobile-menu-item compact" href="/integrations/"><span class="mobile-menu-icon"><UiIcon name="plug" size={18}/></span><span><strong>{nav.integrations}</strong></span></a>
          <a class="mobile-menu-cta" href="/developers/api/"><UiIcon name="api" size={18}/>{nav.freeApi}<span aria-hidden="true">→</span></a>
        </nav>
      </details>
    </div>
  </div>
</header>

<aside id="locale-suggestion" class="hidden fixed z-50 bottom-4 left-4 right-4 sm:left-auto sm:max-w-sm panel rounded-xl p-4 shadow-xl" aria-live="polite">
  <p id="locale-suggestion-text" class="text-sm m-0"></p>
  <div class="flex justify-end gap-2 mt-3">
    <button id="locale-stay" class="soft-button rounded-full px-4 py-2 text-xs" type="button">{nav.stayInEnglish}</button>
    <a id="locale-switch" class="primary-button rounded-full px-4 py-2 text-xs no-underline" href="/">{nav.switchLanguage}</a>
  </div>
</aside>

<script is:inline>
  const root = document.documentElement;
  const saved = localStorage.getItem('theme');
  if (saved !== 'dark') root.classList.add('light');
  // The theme button is #theme-transition-toggle and is wired up further down. An
  // earlier listener here bound to a #theme-toggle element that does not exist.
  const languageMenu = document.querySelector('#language-menu');
  const headerMenus = document.querySelectorAll('[data-header-menu]');
  languageMenu?.querySelectorAll('[data-locale-name]').forEach((link) => {
    link.addEventListener('click', () => localStorage.setItem('preferred-locale', link.getAttribute('data-locale-name') ?? ''));
  });
  document.addEventListener('click', (event) => {
    if (languageMenu instanceof HTMLDetailsElement && !languageMenu.contains(event.target)) languageMenu.open = false;
    headerMenus.forEach((menu) => {
      if (menu instanceof HTMLDetailsElement && !menu.contains(event.target)) menu.open = false;
    });
  });
  document.addEventListener('keydown', (event) => {
    if (event.key === 'Escape') {
      [languageMenu, ...headerMenus].forEach((menu) => {
        if (menu instanceof HTMLDetailsElement && menu.open) {
          menu.open = false;
          menu.querySelector('summary')?.focus();
        }
      });
    }
  });
  const supported = { es: ['es'], ja: ['ja'], de: ['de'], 'pt-BR': ['pt-br', 'pt'] };
  const currentLocale = document.documentElement.lang;
  const dismissed = localStorage.getItem('locale-suggestion-dismissed');
  const preferred = navigator.languages?.map((item) => item.toLowerCase()) ?? [];
  const match = Object.entries(supported).find(([, codes]) => preferred.some((item) => codes.some((code) => item === code || item.startsWith(`${code}-`))));
  if (!dismissed && currentLocale === 'en' && match) {
    const [locale] = match;
    const labels = { es: 'Español', ja: '日本語', de: 'Deutsch', 'pt-BR': 'Português (Brasil)' };
    const panel = document.querySelector('#locale-suggestion');
    const message = document.querySelector('#locale-suggestion-text');
    const link = document.querySelector('#locale-switch');
    if (panel && message && link instanceof HTMLAnchorElement) {
      message.textContent = `Prefer ${labels[locale]}? TokenCalculator.dev is available in your browser language.`;
      link.textContent = `Switch to ${labels[locale]}`;
      link.href = `/${locale}/`;
      panel.classList.remove('hidden');
    }
  }
  document.querySelector('#locale-stay')?.addEventListener('click', () => {
    localStorage.setItem('locale-suggestion-dismissed', 'true');
    document.querySelector('#locale-suggestion')?.classList.add('hidden');
  });
</script>

<script>
  const themeTransitionToggle = document.querySelector('#theme-transition-toggle');

  themeTransitionToggle?.addEventListener('click', async () => {
    const root = document.documentElement;
    const applyTheme = () => {
      root.classList.toggle('light');
      localStorage.setItem('theme', root.classList.contains('light') ? 'light' : 'dark');
    };
    const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    if (!document.startViewTransition || reduceMotion || !(themeTransitionToggle instanceof HTMLElement)) {
      applyTheme();
      return;
    }

    const bounds = themeTransitionToggle.getBoundingClientRect();
    const originX = bounds.left + bounds.width / 2;
    const originY = bounds.top + bounds.height / 2;
    const radius = Math.hypot(
      Math.max(originX, window.innerWidth - originX),
      Math.max(originY, window.innerHeight - originY),
    );

    themeTransitionToggle.setAttribute('disabled', '');
    const transition = document.startViewTransition(applyTheme);
    try {
      await transition.ready;
      root.animate(
        {
          clipPath: [
            `circle(0px at ${originX}px ${originY}px)`,
            `circle(${radius}px at ${originX}px ${originY}px)`,
          ],
        },
        {
          duration: 680,
          easing: 'cubic-bezier(.76, 0, .24, 1)',
          pseudoElement: '::view-transition-new(root)',
        },
      );
      await transition.finished;
    } finally {
      themeTransitionToggle.removeAttribute('disabled');
    }
  });
</script>
