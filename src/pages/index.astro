---
import CalculatorPage from '../components/CalculatorPage.astro';
---
<CalculatorPage title="Token Calculator for Text, Documents & Images | TokenCalculator.dev" description="Count LLM tokens and estimate API cost from pasted text, PDF, DOCX, code, data files, and images. Private browser-only processing for OpenAI, Claude, Gemini, and DeepSeek." canonical="/" eyebrow="Private · browser-only token counter" heading="Count tokens in text, documents, and images." intro="Paste a prompt or attach PDF, DOCX, code, data files, and images. Provider results and costs update live without uploading your content." methodology="OpenAI text uses its exact local BPE encoding. Claude, Gemini, and DeepSeek text results use deterministic calibrated estimates. Supported images follow each provider’s published visual-token rules." />
