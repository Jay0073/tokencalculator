---
import CalculatorPage from '../components/CalculatorPage.astro';
---
<CalculatorPage title="LLM Token Calculator & Input Cost Estimator" description="Count tokens locally for OpenAI, Claude, Gemini, and DeepSeek. Measure text, documents, and images with context-window and input-cost estimates." canonical="/" eyebrow="Private · browser-only token counter" heading="Count tokens across every major provider." intro="Paste text or attach files and images. One click measures the same input for OpenAI, Claude, Gemini, and DeepSeek." methodology="OpenAI text uses its exact local BPE encoding. Claude, Gemini, and DeepSeek text results use deterministic calibrated estimates. Supported images follow each provider’s published visual-token rules." />
