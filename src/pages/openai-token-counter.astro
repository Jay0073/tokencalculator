---
import CalculatorPage from '../components/CalculatorPage.astro';
---
<CalculatorPage provider="openai" title="OpenAI Token Counter — GPT Input Cost Calculator" description="Count GPT tokens locally with the o200k_base tokenizer. Check OpenAI input cost, image tokens, and context-window usage." canonical="/openai-token-counter" eyebrow="OpenAI · exact BPE" heading="Count GPT tokens locally." intro="Measure text and supported files with the o200k_base tokenizer, then check image-token estimates and current input pricing." methodology="OpenAI text is encoded locally with o200k_base, so the raw content count is exact. API message wrappers, tools, and hidden provider instructions are outside the pasted content and can add tokens." />
